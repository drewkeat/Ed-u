default: &default
  adapter: postgresql
  encoding: unicode
  #comment out host and credentials to host locally
  host: db
  username: postgres
  password: password
  pool: 5

development:
  <<: *default
  database: ed-u_development

test:
  <<: *default
  database: ed-u_test
